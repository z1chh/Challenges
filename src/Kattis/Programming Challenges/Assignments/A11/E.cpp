#include <iostream>

using namespace std;

int main()
{
    // Get number of cases
    int num_cases, r, s, x, y, w;
    cin >> num_cases;
    for (int i = 0; i < num_cases; i++)
    {
        // Get input
        cin >> r >> s >> x >> y >> w;

        // TODO
    }

    // Successful return
    return 0;
}
