#include <iostream>
#include <string>

using namespace std;

int main()
{
    // Get input
    int n, k;
    cin >> n >> k;
    string inputs[n];
    for (int i = 0; i < n; i++)
    {
        cin >> inputs[i];
    }

    // LOGIC

    // Successful return
    return 0;
}